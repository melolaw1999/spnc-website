"use client"; import { useState } from "react"; import { useRouter } from "next/navigation";
export function LoginForm(){const [phone,setPhone]=useState("13800000001"),[code,setCode]=useState("123456"),[msg,setMsg]=useState("");const router=useRouter();async function go(e:React.FormEvent){e.preventDefault();setMsg("");const r=await fetch("/api/auth/login",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({phone,code})});const j=await r.json();if(!r.ok)return setMsg(j.error);router.push(j.role==="ADMIN"?"/admin":"/member");router.refresh()}return <form className="form card" onSubmit={go}><div className="notice">当前为本地测试环境，不会真实发送短信。固定验证码：123456</div><label>手机号</label><input className="field" value={phone} onChange={e=>setPhone(e.target.value)} inputMode="numeric"/><label>验证码</label><input className="field" value={code} onChange={e=>setCode(e.target.value)} inputMode="numeric"/><p className="error">{msg}</p><button className="btn" style={{width:"100%"}}>登录</button><p className="muted" style={{fontSize:13}}>会员：13800000001　管理员：13800000000</p></form>}
