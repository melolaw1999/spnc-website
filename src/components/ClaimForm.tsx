"use client";import {useState} from "react";
export function ClaimForm(){const[orderId,setOrder]=useState("TB202606280002"),[last4,setLast4]=useState("1234"),[msg,setMsg]=useState("");async function go(e:React.FormEvent){e.preventDefault();const r=await fetch("/api/claim",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({orderId,last4})});const j=await r.json();setMsg(r.ok?"认领成功，积分状态已同步。":j.error);if(r.ok)setTimeout(()=>location.href="/member/orders",600)}return <form onSubmit={go} className="card form"><label>淘宝订单号</label><input className="field" value={orderId} onChange={e=>setOrder(e.target.value)}/><label>收件手机号后四位</label><input className="field" value={last4} onChange={e=>setLast4(e.target.value)} maxLength={4}/><p className={msg.includes("成功")?"success":"error"}>{msg}</p><button className="btn">核对并认领</button><p className="muted" style={{fontSize:13}}>演示订单 TB202606280002–006 的后四位均为 1234。同一订单只能绑定一个会员。</p></form>}
