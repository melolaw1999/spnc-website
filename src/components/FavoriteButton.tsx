"use client";import{useState}from"react";export function FavoriteButton({productId}:{productId:string}){const[msg,setMsg]=useState("加入收藏");return <button className="btn subtle" onClick={async()=>{const r=await fetch("/api/favorites",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({productId})});const j=await r.json();setMsg(r.ok?(j.favorite?"已收藏":"已取消收藏"):j.error)}}>{msg}</button>}
