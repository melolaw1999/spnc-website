import{db}from"@/lib/db";import{requireUser,maskOrder}from"@/lib/security";export const dynamic="force-dynamic";export default async function Orders(){const u=await requireUser();const orders=await db.externalOrder.findMany({where:{boundUserId:u.id},include:{items:true},orderBy:{orderedAt:'desc'}});return <><h2>我的订单</h2><div className="table-wrap"><table><thead><tr><th>订单号</th><th>下单时间</th><th>商品</th><th>实付金额</th><th>当前状态</th><th>预计积分</th><th>数据来源</th></tr></thead><tbody>{orders.map(o=><tr key={o.id}><td>{maskOrder(o.externalOrderId)}</td><td>{o.orderedAt.toLocaleDateString('zh-CN')}</td><td>{o.items.map(i=>i.title).join('、')}</td><td>¥{(o.buyerPaidAmount/100).toFixed(2)}</td><td>{o.status}</td><td>{Math.floor((o.productPaidAmount-o.refundAmount)/100)}</td><td>淘宝模拟订单</td></tr>)}</tbody></table></div></>}
