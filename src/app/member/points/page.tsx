import{db}from"@/lib/db";import{requireUser}from"@/lib/security";import{getPointSnapshot}from"@/lib/points";export const dynamic="force-dynamic";export default async function Points(){const u=await requireUser();const[s,rows]=await Promise.all([getPointSnapshot(u.id),db.pointLedger.findMany({where:{userId:u.id},orderBy:{createdAt:'desc'}})]);return <><div className="dashboard" style={{marginBottom:30}}><div className="metric">可用<strong>{s.available}</strong></div><div className="metric">待生效<strong>{s.pending}</strong></div><div className="metric">累计获得<strong>{s.earned}</strong></div><div className="metric">已使用<strong>{s.used}</strong></div></div><h2>积分明细</h2><div className="table-wrap"><table><thead><tr><th>时间</th><th>类型</th><th>说明</th><th>状态</th><th>变动</th></tr></thead><tbody>{rows.map(x=><tr key={x.id}><td>{x.createdAt.toLocaleString('zh-CN')}</td><td>{x.type}</td><td>{x.description}</td><td>{x.status}</td><td style={{color:x.amount>=0?'#067647':'#b42318'}}>{x.amount>0?'+':''}{x.amount}</td></tr>)}</tbody></table></div><p className="muted">积分通过流水汇总计算；订单待生效、正式生效、退款扣回、管理员调整、兑换与过期均保留独立记录。</p></>}
