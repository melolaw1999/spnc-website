# 网站素材目录

这里的文件全部是本机原始素材的项目内复制件。不要通过网站后台删除、覆盖或回写本机原始文件。

- `brand/`：理想营养主品牌和萨瑟恩公司主体素材。
- `products/`：按品牌和产品线分类的产品图。
- `optimized/`：由上述项目复制件生成的 WebP 网页版本；原图仍保留在前两个目录。

产品图在页面中统一使用 `object-fit: contain`，保持原始宽高比。公开官网商品映射维护在 `src/data/catalog.ts`；本地素材后台仍使用数据库 `Asset`、`ProductImage`。
